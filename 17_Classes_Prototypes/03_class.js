/* 
class
  The class declaration creates a new class with a given name using prototype-based inheritance

*/

// Example 1
class Color {
	constructor(r, g, b) {
		this.r = r;
		this.g = g;
		this.b = b;
	}
}
